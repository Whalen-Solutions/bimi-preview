services:
  bimi-preview:
    build: .
    container_name: bimi-preview
    ports:
      - "5000:8000"
    env_file: .env
    volumes:
      - uploads:/app/uploads
    restart: unless-stopped

volumes:
  uploads:
